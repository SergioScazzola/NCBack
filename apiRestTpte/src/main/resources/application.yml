

spring:
 security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://cognito-idp.us-east-2.amazonaws
          
aws:
  region: us-east-2
application:
    name: apiRestTpte
datasource:
    url: jdbc:mysql://${HOST_DB}:${PORT_DB}/lognc?useSSL=false
    username: ${USER_DB}
    password: ${PASSWORD_DB}

server:
  port: ${PORT_SERVER:5000}

app:
  frontend-url: ${FRONTEND_URL}

afip:
  wsaa:
    endfev: http://ar.gov.afip.dif.FEV1/
    endpoint: https://wsaa.afip.gov.ar/ws/services/LoginCms
    service: wsfe
    keystore: C:/CERTIF/bulo24.p12
    ksigner: 1
    password: 1234
    isProduction: 1
    dstDn: "cn=wsaa,o=afip,c=ar,serialNumber=CUIT 33693450239"  
    TicketTime: 36000




